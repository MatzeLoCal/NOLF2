// macOS stub for Windows resource.h (control IDs from the .rc files). The macOS
// build has no Win32 resources; anything that genuinely needs an ID will surface
// at compile time and can be added here.
#ifndef __LT_COMPAT_RESOURCE_H__
#define __LT_COMPAT_RESOURCE_H__
#endif
