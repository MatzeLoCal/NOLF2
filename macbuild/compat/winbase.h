// Stub of <winbase.h> for the macOS build: on Windows it is a sub-header of
// <windows.h>; everything the code needs comes from the windows.h shim.
#ifndef __LT_COMPAT_WINBASE_H__
#define __LT_COMPAT_WINBASE_H__
#include <windows.h>
#endif
